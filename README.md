# 剪球

剪球是一个面向球类运动爱好者的本地运动剪辑 MVP。当前版本使用 Python、OpenCV 和 NumPy 对真实视频帧进行分析，可以在浏览器里完成视频载入、球轨迹追踪、疑似击球检测、冗余片段建议、击球特效预览、精彩瞬间时间线、带水印预览导出和剪辑决策 JSON 下载。

当前实现是本地最小价值单元：视频不会上传到外部服务器，页面和处理逻辑都运行在 `127.0.0.1`。

## 环境要求

- Node.js 18 或更高版本
- Python 3.10 或更高版本
- Python 包：`opencv-python`、`numpy`

安装 Python 依赖：

```powershell
python -m pip install opencv-python numpy
```

## 运行方式

进入应用目录：

```powershell
cd app
npm start
```

然后打开：

```text
http://127.0.0.1:4173/
```

Windows 用户也可以直接双击根目录的 `start-jianqiu.cmd`。请保持启动窗口开启，关闭窗口会停止本地服务。

需要停止后台服务时，可运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\stop-jianqiu.ps1
```

## 当前功能

- 本地视频上传与预览
- 生成演示视频，方便无素材时测试流程
- 运动类型选择：网球、羽毛球、乒乓球、篮球、足球、高尔夫
- 剪辑目标选择：综合剪辑、删除捡球等待、精彩瞬间、击球特效
- 从真实视频帧生成球轨迹、疑似击球点、冗余片段和精彩片段
- 播放时跳过冗余片段
- Canvas 击球特效预览
- 导出带水印 WebM 预览视频
- 下载剪辑决策 JSON

## 当前识别判据

- 球候选：颜色范围与帧间运动区域相交，再按面积、圆度、边缘距离和轨迹连续性评分。
- 球轨迹：候选点必须与上一位置或预测位置保持合理距离；短时丢失后会终止当前轨迹，避免跨场景误连。
- 球拍类击球：连续轨迹中球速达到最低门槛，同时前后运动方向出现显著反向变化。
- 其他运动关键动作：允许使用显著方向变化，或伴随明显速度倍率变化的加速事件。
- 冗余片段：持续低运动量、没有移动球轨迹、也没有击球事件，并超过用户选择的时长阈值。
- 精彩片段：置信度不低于 0.58 的疑似击球事件前 2.2 秒至后 3 秒。

事件结果会保存方向变化角度、前后速度、加速度和轨迹连续度。没有形成可靠轨迹时，系统返回明确警告，不会生成模拟击球。

## 自动测试

```powershell
cd app
npm test
```

测试会自动生成已知击球时间和等待区间的合成网球视频，并验证：

- 4 个击球全部命中
- 平均击球时间误差
- 轨迹覆盖率
- 等待区间识别
- 无球视频不得产生击球
- 本地分析接口可用
- 临时视频处理后被删除
- 前后端脚本语法

## 已知限制

- 当前是基于运动、颜色和轨迹几何的 OpenCV 版本，还没有加入人体姿态模型和球拍检测模型。
- 球太小、严重模糊、与背景同色、镜头快速移动或光线变化剧烈时，轨迹可能中断。
- 选择正确运动类型很重要，因为不同球类使用不同颜色和尺寸范围。
- “疑似击球”是轨迹变化判断，不等同于自动计分或裁判级识别。

## 项目结构

```text
app/
  index.html   页面结构
  styles.css   界面样式
  app.js       本地视频分析演示、时间线、特效和导出逻辑
  server.js    本地静态服务
analyzer/
  analyze_video.py  OpenCV 本地视频分析器
tests/
  generate_synthetic_video.py  合成测试视频
  test_analyzer.py             识别准确性回归测试
  test_api.py                  本地接口与临时文件测试
```

## 后续方向

- 接入真实人体姿态识别、球检测、球拍/器械检测模型
- 使用 FFmpeg 或服务端渲染导出高清 MP4
- 增加用户项目管理、付费导出、云端处理队列
- 针对不同运动训练独立识别模型

## 其他目录

仓库中还包含 Codex 协作配置目录，例如 `AGENTS.md`、`CLAUDE.md/`、`skills/`、`hooks/`、`plugins/`、`subagents/` 和 `scripts/`。这些文件用于辅助后续开发协作。
